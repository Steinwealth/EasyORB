# Trading Parameters Configuration
# ================================

# Strategy Configuration
STRATEGY_MODE=standard
AUTOMATION_MODE=demo
TRADING_ENABLED=true

# Position Management (OPTIMIZED FOR MAXIMUM PROFIT CAPTURE)
MAX_OPEN_POSITIONS=20
BASE_POSITION_SIZE_PCT=10.0  # 10% base position size to match Live Mode
MAX_POSITION_SIZE_PCT=35.0   # Maximum position size after all boosts
MIN_POSITION_SIZE_PCT=1.0
MIN_POSITION_VALUE=50.0
POSITION_SIZING_METHOD=confidence_based_dynamic

# Rev 00102: Capital allocation in configs/strategies.env (SINGLE SOURCE OF TRUTH)
# SO_CAPITAL_PCT=90, CASH_RESERVE_PCT=10, ORR_CAPITAL_PCT=0
POSITION_SPLITTING_ENABLED=true

# Risk Management
MAX_RISK_PER_TRADE_PCT=10.0
MAX_DRAWDOWN_PCT=10.0
SAFE_MODE_ENABLED=true

# Order Management
ORDER_TYPE=market
TIME_IN_FORCE=day
ALLOW_PARTIAL_FILLS=true
AUTO_CANCEL_ORDERS=true

# Signal Configuration
SIGNAL_CONFIDENCE_THRESHOLD=0.75
SIGNAL_QUALITY_THRESHOLD=medium
SIGNAL_VALIDATION_ENABLED=true

# Risk Management (OPTIMIZED FOR MAXIMUM PROFIT CAPTURE)
MAX_DAILY_LOSS_PCT=2.0
MAX_WEEKLY_LOSS_PCT=5.0
MAX_MONTHLY_LOSS_PCT=10.0
STOP_LOSS_ATR_MULTIPLIER=1.5
TAKE_PROFIT_ATR_MULTIPLIER=3.0

# Optimized Take Profit Targets
BASE_TAKE_PROFIT_PCT=5.0      # 5% base take profit (vs 2% before)
TRENDING_TAKE_PROFIT_PCT=12.0 # 12% trending moves
EXPLOSIVE_TAKE_PROFIT_PCT=25.0 # 25% explosive moves (vs 10% before)
MOON_TAKE_PROFIT_PCT=50.0     # 50% moon moves (vs 25% before)

# Trading Hours
TRADING_START_TIME=09:30
TRADING_END_TIME=16:00
PREMARKET_ENABLED=true
AFTER_HOURS_ENABLED=false

# Market Conditions
MIN_VOLUME_THRESHOLD=100000
MIN_PRICE_THRESHOLD=5.0
MAX_PRICE_THRESHOLD=1000.0
MIN_MARKET_CAP=1000000000

# Performance Targets (OPTIMIZED FOR MAXIMUM PROFIT CAPTURE)
TARGET_DAILY_RETURN_PCT=29.29  # 292x improvement from 0.10% to 29.29% average P&L per trade
TARGET_WEEKLY_RETURN_PCT=160.0  # 8-12 trades per day at 20-30% each = 160-360% weekly
TARGET_MONTHLY_RETURN_PCT=360.0 # Optimized monthly targets
MIN_WIN_RATE=0.95               # 95% minimum win rate (vs 80% before)
MIN_PROFIT_FACTOR=100.0         # 100x minimum profit factor (vs 3x before)

# Symbol List Configuration (Rev 00173) ⭐
# ARCHIVED: Symbol selection no longer used - all symbols from core_list.csv used
# SYMBOL_SELECTION_METHOD=static  # ❌ ARCHIVED
CORE_LIST_PATH=data/watchlist/core_list.csv  # ⭐ PRIMARY: 84 elite symbols
CORE_SYMBOLS_COUNT=84  # All symbols used for ORB capture
# DYNAMIC_SYMBOLS_ENABLED=false  # ❌ ARCHIVED
MAX_SYMBOLS_TO_TRADE=25

# News Integration
NEWS_SENTIMENT_ENABLED=true
NEWS_SENTIMENT_THRESHOLD=0.3
NEWS_IMPACT_WEIGHT=0.2

# Technical Analysis
RSI_OVERSOLD=30
RSI_OVERBOUGHT=70
MACD_SIGNAL_THRESHOLD=0.1
VOLUME_SURGE_THRESHOLD=1.5
PRICE_MOMENTUM_THRESHOLD=0.02

# Execution Settings
EXECUTION_DELAY_MS=100
MAX_RETRY_ATTEMPTS=3
RETRY_DELAY_MS=1000
ORDER_TIMEOUT_SECONDS=30

# Monitoring
PERFORMANCE_MONITORING_ENABLED=true
ALERT_ON_ERRORS=true
ALERT_ON_LOSSES=true
ALERT_ON_GAINS=true
DAILY_SUMMARY_ENABLED=true

# Logging
LOG_LEVEL=INFO
LOG_TRADES=true
LOG_SIGNALS=true
LOG_PERFORMANCE=true
LOG_ERRORS=true
